<LessonHero
 title="Convars: server.cfg config you can read from Lua"
 meta={[
 { label: "You'll learn", value: "The three convar prefixes (set, setr, sets), who can read each, the read functions, and why a client can only see a setr value." },
 { label: "You'll ship", value: "A debug toggle and a configurable starting balance for the banking script: both flippable from server.cfg with no code redeploy." },
 { label: "Time", value: "~15 minutes" },
 { label: "Why now", value: "You finished the banking lesson with a hard-coded starting balance. Convars let you tune that per server." },
 ]}
>
 A **convar** (configuration variable) is a key/value pair you set in `server.cfg`, then read from Lua. Convars hold the things that change per server: a debug flag, a database name, a starting balance, an owner ID. You tune them without editing Lua and without rebuilding the resource. One thing trips up everyone: the prefix you declare a convar with decides whether a client can read it. Get it wrong and your client silently reads the default forever. This lesson makes the difference visible.
</LessonHero>

<Prereq
 items={[
 "I can find my server resources folder.",
 "I can edit server.cfg and restart one resource.",
 "I can read txAdmin Live Console and F8 output."
 ]}
/>

<Callout variant="fivem" title="Build the proof, then understand it">
 This lesson builds server.cfg convars read from both server and client Lua, as a working FiveM resource named `qu_convars_server_cfg`. Follow the folder names, file names, code, restart command, and expected output exactly. Once it works, the next section takes it apart so you understand why a client can read one convar but not the other.
</Callout>

## Build it

<StepGroup>
 <Step num={1} title="Make the resource folder" outcome="The server has one folder for this lesson.">
 Inside your server's `resources` folder, create this folder:

 ```text
 resources/qu_convars_server_cfg
 ```
 </Step>

 <Step num={2} title="Create the files" outcome="Every file named in the manifest exists.">
 Create this exact file layout:

 ```text
resources/qu_convars_server_cfg/
 fxmanifest.lua
 server.lua
 client.lua
 ```
 </Step>

 <Step num={3} title="Write fxmanifest.lua" outcome="FiveM knows which files to load.">
 Open `fxmanifest.lua` and paste this:

 ```lua
fx_version 'cerulean'
game 'gta5'

server_script 'server.lua'
client_script 'client.lua'
 ```

 Older tutorials add a `lua54 'yes'` line here. As of June 2025 that setting is deprecated and ignored: Lua 5.4 is now the only Lua runtime, so you leave it out.
 </Step>

 <Step num={4} title="Declare the convars in server.cfg" outcome="The server has two config values it can read.">
 Open `server.cfg` and add these two lines **above** the line that ensures this resource. You will add the `ensure` line in Step 6.

 `server.cfg` is NOT inside the `resources` folder. It lives in your server's base data directory, the same folder you launch FXServer from, one level above `resources`. (If you set the server up with txAdmin, you can also open it from the txAdmin **Settings** page.)

 ```text
set qu_convars_cash 750
setr qu_convars_debug true
 ```

 The two prefixes are not interchangeable. `set` is server-only. `setr` is replicated, which means it is also sent to every client. You will see in a moment why the client half of this lesson reads the `setr` one and not the `set` one.
 </Step>

 <Step num={5} title="Write the lesson code" outcome="The topic is now represented by runnable code.">
 Open `server.lua` and paste this:

 ```lua
RegisterCommand('cashcfg', function()
 local cash = GetConvarInt('qu_convars_cash', 500)
 print('[qu_convars_server_cfg] server reads cash ' .. cash)
end, false)
 ```

 Open `client.lua` and paste this:

 ```lua
RegisterCommand('debugcfg', function()
 local debug = GetConvar('qu_convars_debug', 'false')
 print('[qu_convars_server_cfg] client reads debug ' .. debug)
end, false)
 ```
 </Step>

 <Step num={6} title="Start and test it" outcome="The expected proof appears in the correct console.">
 Add this line to `server.cfg`, below the two convar lines from Step 4:

 ```text
 ensure qu_convars_server_cfg
 ```

 Save, then restart the whole server so FXServer re-reads `server.cfg`. In txAdmin (the web control panel at `http://<your-server-ip>:40120`, or `http://localhost:40120` on the server machine), open the dashboard and click **Restart** (the server restart, not a single-resource restart). A resource restart alone does NOT re-execute changed `server.cfg` lines, so the new values would not load.

 Run the first test in the txAdmin Live Console:

 ```text
 cashcfg
 ```

 Join the server. While you are in-game, press the **F8** key to open the FiveM developer console (a text overlay that drops down over the game). This is where client-side `print` output appears and where you type client commands. With it open, run the second test there:

 ```text
 debugcfg
 ```

 <ExpectedOutput>
 [qu_convars_server_cfg] server reads cash 750
F8: [qu_convars_server_cfg] client reads debug true
 </ExpectedOutput>
 </Step>
</StepGroup>

## How it works

It worked, so now make it make sense. Two convars, two read functions, two consoles. The split is the whole point: the prefix you declared each convar with decided whether the client could ever see it.

### The three prefixes, and who can read each

When you write a convar in `server.cfg` you pick one of three commands. They look almost identical and behave completely differently.

- `set name value` is a **server-only** convar. The server reads it. Clients cannot: a client read returns the fallback default, not the real value. Use it for anything a player should never see: database strings, API keys, owner IDs.
- `setr name value` is a **replicated** convar (the `r`). The server reads it, and the value is also pushed to every client, so client Lua reads the real value too. Use it only for config that is safe for players to know and that the client needs, like a debug flag.
- `sets name value` is a **server-info** convar. Its name and value appear on the server list and in the public `info.json`. Treat every `sets` value as public; never put a secret in one.

That is the whole rule. `set` stays on the server, `setr` rides along to clients, `sets` is for the browser. In this lesson `qu_convars_cash` is a `set` (a player has no business reading the starting balance) and `qu_convars_debug` is a `setr` (the client console needs to know whether debug logging is on).

<Checkpoint question="If you change qu_convars_debug from setr to set and restart, what does the F8 line print, and why?">
 It prints `client reads debug false`. With plain `set`, the convar is server-only and never replicates to clients, so on the client side `GetConvar('qu_convars_debug', 'false')` cannot find a value and returns the fallback you supplied, the string `'false'`. The server console would still read the real value, but the client is permanently blind to a non-replicated convar. This is the number one convar bug: a value that "works on the server" but silently reads the default on every client because it was declared with `set` instead of `setr`.
</Checkpoint>

### The read functions

You read a convar with a `GetConvar*` native. They all take the same two arguments, the convar name and a fallback default, and differ only in the type they return.

- `GetConvar(name, default)` returns a **string**. Convar values are strings at heart, so this is the general reader.
- `GetConvarInt(name, default)` returns an **integer**, parsed from the stored text. The server side of this lesson uses it on `qu_convars_cash` because a balance is a number, not text.
- `GetConvarFloat(name, default)` returns a **float**, for decimals like a price multiplier.
- `GetConvarBool(name, default)` returns a **boolean**.

The default is your safety net. If the convar is missing, misspelled, or (the case you met) declared with the wrong prefix so the client cannot see it, the read returns the default instead of crashing. That is why the F8 line prints `false` when the convar does not reach the client: `'false'` is the default you handed `GetConvar`.

### The restricted flag on the command

Both commands here end with `false`:

```lua
RegisterCommand('cashcfg', function()
 -- ...
end, false)
```

That last argument is the **restricted flag**. With `false`, anyone can run the command, including the server console and any player. With `true`, the command needs the `command.<name>` ace, so only the console (which holds all aces) and authorized admins can run it. For a teaching command you want to fire freely, `false` is correct. When you later wrap a real admin action like granting cash, switch to `true` and grant the ace so players cannot run it on themselves.

### Runtime versus server.cfg

One more model to lock in. Editing the `server.cfg` **file** does not run the changed line on a live server. Restarting only the resource does not re-read it either. To load the new value, fully restart the server or re-run the file with `exec server.cfg`. The `GetConvar*` calls read the current value each time they run, so you can also set a convar live from the server console:

```text
setr qu_convars_debug false
```

A live `set`/`setr` takes effect immediately, no restart needed, and a `setr` change is pushed to connected clients right away. The cfg line is the value the convar starts at on boot.

### When you reach for this pattern

Convars are how you stop hard-coding the things that differ between your test server and a live server. Three real cases:

- **A debug switch.** Wrap your noisy `print` calls in `if GetConvarBool('myresource_debug', false) then`. Flip `setr myresource_debug true` while you investigate, flip it off in production, and you never ship a build to change logging.
- **Per-server tuning.** Starting balance, max stack size, payout rates. One resource, many servers, each with its own numbers in its own `server.cfg`.
- **Secrets that must stay server-side.** A Tebex secret, a Discord webhook, a database connection string. Always a plain `set`, never `setr`. A `setr` convar is copied to every client, where any player can read it out of memory, so a secret declared with `setr` is a real credential leak. Keep secrets as `set` so they never leave the server.

## If something went wrong

<FailureList
 items={[
 { symptom: "Server reads cash 500 instead of 750", fix: "The convar is missing or misspelled, or you edited server.cfg without re-executing it. Fix the line, then fully restart the server (or deliberately exec the cfg) before testing again." },
 { symptom: "F8 client reads debug false (not true)", fix: "The convar was declared with set instead of setr, or the edited cfg was never re-executed. Use setr, then fully restart the server or exec the cfg." },
 { symptom: "debugcfg prints nothing in F8", fix: "Client convars are read on the client, so you must join the server and run debugcfg in the F8 console, not in txAdmin. txAdmin shows server output only." },
 { symptom: "cashcfg prints nothing", fix: "Run cashcfg in the txAdmin Live Console. If you had left the restricted flag as true, only the console and authorized aces could run it; this lesson uses false so it runs anywhere." },
 ]}
/>

## What you can do now

<Recap
 items={[
 "Name the three convar prefixes and say who can read each: set is server-only, setr replicates to clients, sets is the server-browser info string.",
 "Explain why a client reading a plain set convar silently gets the default, and that setr is required for any value client Lua must see.",
 "Pick the right read function by return type: GetConvar (string), GetConvarInt (int), GetConvarFloat (float), GetConvarBool (bool), each with a fallback default.",
 "Declare config in server.cfg and read it from Lua so a value changes per server with no code redeploy.",
 "Explain that editing a cfg file does not execute it; restart the server or exec the cfg, while a live set in the server console applies immediately.",
 ]}
/>

## Common failures

| What you see | What it means | How to fix it |
|---|---|---|
| ConVar change doesn't take effect after restart | The resource sets the value in code after startup, overwriting server.cfg | Resources can call `SetConvar()` at runtime. server.cfg sets the initial value, but code wins if it runs after |
| `set` vs `setr` vs `sets` confusion | Each does something different | `set` = server-only value, `setr` = replicated to all clients, `sets` = publicly exposed (server browser and info.json). Never put secrets in `sets` |
| License key in a public GitHub repo | You committed server.cfg with the key in it | Rotate the key immediately at portal.cfx.re. Add server.cfg to .gitignore |
| ConVar value won't change no matter what you do | It's a restricted engine ConVar (like `sv_licenseKey`) | Engine-protected ConVars can only be set in server.cfg at startup. They cannot be changed at runtime |

## Try it yourself

<Exercise>
 Add a third convar and prove the replication rule with your own hands. Put a server-only secret next to the existing lines in `server.cfg`:

 ```text
 set qu_convars_secret supersecret
 ```

 Now try to read it from **both** sides. Add to `server.lua`:

 ```lua
 RegisterCommand('secretcfg_server', function()
 print('[qu_convars_server_cfg] server secret ' .. GetConvar('qu_convars_secret', 'hidden'))
 end, false)
 ```

 And add the matching client command to `client.lua`:

 ```lua
 RegisterCommand('secretcfg_client', function()
 print('[qu_convars_server_cfg] client secret ' .. GetConvar('qu_convars_secret', 'hidden'))
 end, false)
 ```

 Fully restart the server so it re-reads `server.cfg`. Run `secretcfg_server` in txAdmin and `secretcfg_client` in F8. The server prints `server secret supersecret`. The client prints `client secret hidden`, because a plain `set` convar never crosses to the client and the read falls back to your default. You have demonstrated, in your own console, why every secret stays a `set` and never a `setr`.
</Exercise>

<Refs
 items={[
 { label: "FiveM convars reference", href: "https://docs.fivem.net/docs/scripting-reference/convars/" },
 { label: "FiveM server.cfg / setting up a server", href: "https://docs.fivem.net/docs/server-manual/setting-up-a-server/" },
 { label: "FiveM resource manifest reference", href: "https://docs.fivem.net/docs/scripting-reference/resource-manifest/" }
 ]}
/>
