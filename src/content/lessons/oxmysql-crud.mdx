<LessonHero
  crumbs="Track 2 · Pro Path"
  title="Database with oxmysql: CRUD without the footguns"
  meta={[
    { label: "You'll learn", value: "query, scalar, insert, update, single, prepare, parameter placeholders, pool sizing" },
    { label: "Time", value: "~25 minutes" },
    { label: "Prereqs", value: "Lesson 14 (server events), Bonus 13c (cancel patterns)" },
    { label: "Outcome", value: "You can build a server-authoritative ban system, save player money on disconnect, and never concatenate SQL again" },
  ]}
>
  Almost every server you build will save state. Bans, money, garages, jobs, all of it lives in MySQL. `oxmysql` is the modern, async-friendly driver everyone uses. This lesson teaches the four query helpers, why prepared statements stop attackers cold, and how to size the connection pool so you do not deadlock at 32 players.
</LessonHero>

<Prereq
  items={[
    "I can find my server resources folder.",
    "I can edit server.cfg and restart one resource.",
    "I can read txAdmin Live Console and F8 output."
  ]}
/>

<Callout variant="fivem" title="Build the proof, then change it">
  This lesson builds oxmysql insert, select, update, and delete as a working FiveM resource named `qu_oxmysql_crud`. Follow the folder names, file names, code, restart command, and expected output exactly.
</Callout>

## Build it

<StepGroup>
  <Step num={1} title="Make the resource folder" outcome="The server has one folder for this lesson.">
    Inside your server's `resources` folder, create this folder:

    ```text
    resources/qu_oxmysql_crud
    ```
  </Step>

  <Step num={2} title="Create the files" outcome="Every file named in the manifest exists.">
    Create this exact file layout:

    ```text
resources/qu_oxmysql_crud/
  fxmanifest.lua
  server.lua
    ```
  </Step>

  <Step num={3} title="Write fxmanifest.lua" outcome="FiveM knows which files to load.">
    Open `fxmanifest.lua` and paste this:

    ```lua
fx_version 'cerulean'
game 'gta5'

server_script '@oxmysql/lib/MySQL.lua'
server_script 'server.lua'

dependency 'oxmysql'
    ```

    Run this SQL before starting the resource:

    ```sql
CREATE TABLE IF NOT EXISTS qu_oxmysql_crud_rows (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(80) NOT NULL,
  amount INT NOT NULL DEFAULT 0
);
    ```
  </Step>

  <Step num={4} title="Write the lesson code" outcome="The topic is now represented by runnable code.">
    Open `server.lua` and paste this:

    ```lua
RegisterCommand('dbtest', function(src)
    local id = MySQL.insert.await('INSERT INTO qu_oxmysql_crud_rows (name, amount) VALUES (?, ?)', { 'lesson', 100 })
    local row = MySQL.single.await('SELECT * FROM qu_oxmysql_crud_rows WHERE id = ?', { id })
    print('[qu_oxmysql_crud] selected ' .. row.name .. ' ' .. row.amount)
    MySQL.update.await('UPDATE qu_oxmysql_crud_rows SET amount = ? WHERE id = ?', { 150, id })
    print('[qu_oxmysql_crud] updated ' .. id)
end, true)
    ```
  </Step>

  <Step num={5} title="Start and test it" outcome="The expected proof appears in the correct console.">
    Open `server.cfg` and add this line:

    ```text
    ensure qu_oxmysql_crud
    ```

    Save, then run:

    ```text
    restart qu_oxmysql_crud
    ```

    Run this test:

    ```text
    dbtest
    ```

    <ExpectedOutput>
      [qu_oxmysql_crud] selected lesson 100
[qu_oxmysql_crud] updated rowId
    </ExpectedOutput>
  </Step>
</StepGroup>

## If something went wrong

<FailureList
  items={[
    { symptom: "Resource qu_oxmysql_crud does not exist", fix: "Check the folder is inside resources and the ensure line uses the exact same name." },
    { symptom: "Could not load resource qu_oxmysql_crud", fix: "Open fxmanifest.lua and fix the first quote, brace, or file name error the console reports." },
    { symptom: "The command does not print", fix: "Run restart qu_oxmysql_crud, then run the test command in the console named in the step." },
    { symptom: "Client output is missing", fix: "Join the server first and read F8. Server output stays in txAdmin Live Console." },
  ]}
/>

## What you can do now

<Recap
  items={[
    "Make the qu_oxmysql_crud folder and every file by hand.",
    "Use fxmanifest.lua to load the exact files for oxmysql insert, select, update, and delete.",
    "Add ensure qu_oxmysql_crud to server.cfg and restart only that resource.",
    "Read the expected console output before changing the code."
  ]}
/>

## Try it yourself

<Exercise>
  Change one printed value, restart `qu_oxmysql_crud`, rerun the test, and confirm the output changed.
</Exercise>

<Refs
  items={[
    { label: "FiveM resource manifest reference", href: "https://docs.fivem.net/docs/scripting-reference/resource-manifest/resource-manifest/" },
    { label: "FiveM native reference", href: "https://docs.fivem.net/natives/" },
    { label: "Lua 5.4 reference manual", href: "https://www.lua.org/manual/5.4/" }
  ]}
/>
