<LessonHero
  crumbs="Module 01 · Lesson 01"
  title="Your first line of Lua"
  meta={[
    { label: "You'll build", value: "A tiny resource that prints a message to the server console when it starts." },
    { label: "Time", value: "~12 minutes" },
    { label: "You need", value: "A local FiveM practice setup you can restart, and a text editor (VS Code is free). If you do not have that yet, do the 7-day browser starter first, then come back here." },
    { label: "You'll learn", value: "Lua syntax basics -> strings -> variables -> the print function -> how to use the console as feedback" },
  ]}
>
  Every coder starts the same way: one line, one message, one visible result. In this lesson that result is *Hello, Los Santos* in your FiveM server console. By the end you will understand a **string**, a **variable**, and the `print` function.
</LessonHero>

<Prereq
  items={[
    "I can find my server resources folder.",
    "I can edit server.cfg and restart one resource.",
    "I can read txAdmin Live Console and F8 output."
  ]}
/>

<Callout variant="fivem" title="Build the proof, then change it">
  This lesson builds one server print from one Lua file as a working FiveM resource named `qu_first_line_of_lua`. Follow the folder names, file names, code, restart command, and expected output exactly.
</Callout>

## Build it

<StepGroup>
  <Step num={1} title="Make the resource folder" outcome="The server has one folder for this lesson.">
    Inside your server's `resources` folder, create this folder:

    ```text
    resources/qu_first_line_of_lua
    ```
  </Step>

  <Step num={2} title="Create the files" outcome="Every file named in the manifest exists.">
    Create this exact file layout:

    ```text
resources/qu_first_line_of_lua/
  fxmanifest.lua
  server.lua
    ```
  </Step>

  <Step num={3} title="Write fxmanifest.lua" outcome="FiveM knows which files to load.">
    Open `fxmanifest.lua` and paste this:

    ```lua
fx_version 'cerulean'
game 'gta5'

server_script 'server.lua'
    ```

    Older tutorials add a `lua54 'yes'` line here. As of June 2025 that setting is deprecated and ignored: Lua 5.4 is now the only Lua runtime, so you leave it out.
  </Step>

  <Step num={4} title="Write the lesson code" outcome="The topic is now represented by runnable code.">
    Open `server.lua` and paste this:

    ```lua
RegisterCommand('first_line_of_lua', function()
    print('[qu_first_line_of_lua] proof line')
end, true)
    ```
  </Step>

  <Step num={5} title="Start and test it" outcome="The expected proof appears in the correct console.">
    Open `server.cfg` and add this line:

    ```text
    ensure qu_first_line_of_lua
    ```

    Save, then run:

    ```text
    restart qu_first_line_of_lua
    ```

    Run this test:

    ```text
    first_line_of_lua
    ```

    <ExpectedOutput>
      [qu_first_line_of_lua] proof line
    </ExpectedOutput>
  </Step>
</StepGroup>

## If something went wrong

<FailureList
  items={[
    { symptom: "Resource qu_first_line_of_lua does not exist", fix: "Check the folder is inside resources and the ensure line uses the exact same name." },
    { symptom: "Could not load resource qu_first_line_of_lua", fix: "Open fxmanifest.lua and fix the first quote, brace, or file name error the console reports." },
    { symptom: "The command does not print", fix: "Run restart qu_first_line_of_lua, then run the test command in the console named in the step." },
    { symptom: "Client output is missing", fix: "Join the server first and read F8. Server output stays in txAdmin Live Console." },
  ]}
/>

## What you can do now

<Recap
  items={[
    "Make the qu_first_line_of_lua folder and every file by hand.",
    "Use fxmanifest.lua to load the exact files for one server print from one Lua file.",
    "Add ensure qu_first_line_of_lua to server.cfg and restart only that resource.",
    "Read the expected console output before changing the code."
  ]}
/>

## Run it live

No server needed for this part. Edit the Lua below and press RUN to execute it in your browser on real Lua 5.4. Change the message, add a second `print`, and watch the output.

<Sandbox initial={`print("Hello, Los Santos")
print("My first line of Lua")`} />

## Try it yourself

<Exercise>
  Change one printed value, restart `qu_first_line_of_lua`, rerun the test, and confirm the output changed.
</Exercise>

<Refs
  items={[
    { label: "FiveM resource manifest reference", href: "https://docs.fivem.net/docs/scripting-reference/resource-manifest/resource-manifest/" },
    { label: "FiveM native reference", href: "https://docs.fivem.net/natives/" },
    { label: "Lua 5.4 reference manual", href: "https://www.lua.org/manual/5.4/" }
  ]}
/>
