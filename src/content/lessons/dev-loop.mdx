<LessonHero
  crumbs="Module 02 · Lesson 09"
  title="Start, restart, stop: your daily dev loop"
  meta={[
    { label: "You'll build", value: "A repeatable workflow: edit -> save -> ensure -> read console -> test." },
    { label: "Time", value: "~15 minutes" },
    { label: "You need", value: "The qu_hello resource from Lessons 07 and 08, and a running FiveM server." },
    { label: "You'll learn", value: "The four commands -> reading the server console -> the dev loop -> server.cfg -> common error patterns" },
  ]}
>
  FiveM development is mostly one small loop: edit, save, `ensure`, read the console, test. Beginners often think they need more tricks than that. They do not. This lesson is about making that loop feel normal and easy.
</LessonHero>

<Prereq
  items={[
    "I can find my server resources folder.",
    "I can edit server.cfg and restart one resource.",
    "I can read txAdmin Live Console and F8 output."
  ]}
/>

<Callout variant="fivem" title="Build the proof, then change it">
  This lesson builds edit, save, restart one resource, and verify one proof line as a working FiveM resource named `qu_dev_loop`. Follow the folder names, file names, code, restart command, and expected output exactly.
</Callout>

## Build it

<StepGroup>
  <Step num={1} title="Make the resource folder" outcome="The server has one folder for this lesson.">
    Inside your server's `resources` folder, create this folder:

    ```text
    resources/qu_dev_loop
    ```
  </Step>

  <Step num={2} title="Create the files" outcome="Every file named in the manifest exists.">
    Create this exact file layout:

    ```text
resources/qu_dev_loop/
  fxmanifest.lua
  server.lua
    ```
  </Step>

  <Step num={3} title="Write fxmanifest.lua" outcome="FiveM knows which files to load.">
    Open `fxmanifest.lua` and paste this:

    ```lua
fx_version 'cerulean'
game 'gta5'

server_script 'server.lua'
    ```
  </Step>

  <Step num={4} title="Write the lesson code" outcome="The topic is now represented by runnable code.">
    Open `server.lua` and paste this:

    ```lua
RegisterCommand('dev_loop', function()
    print('[qu_dev_loop] proof line')
end, true)
    ```
  </Step>

  <Step num={5} title="Start and test it" outcome="The expected proof appears in the correct console.">
    Open `server.cfg` and add this line:

    ```text
    ensure qu_dev_loop
    ```

    Save, then run:

    ```text
    restart qu_dev_loop
    ```

    Run this test:

    ```text
    dev_loop
    ```

    <ExpectedOutput>
      [qu_dev_loop] proof line
    </ExpectedOutput>
  </Step>
</StepGroup>

## If something went wrong

<FailureList
  items={[
    { symptom: "Resource qu_dev_loop does not exist", fix: "Check the folder is inside resources and the ensure line uses the exact same name." },
    { symptom: "Could not load resource qu_dev_loop", fix: "Open fxmanifest.lua and fix the first quote, brace, or file name error the console reports." },
    { symptom: "The command does not print", fix: "Run restart qu_dev_loop, then run the test command in the console named in the step." },
    { symptom: "Client output is missing", fix: "Join the server first and read F8. Server output stays in txAdmin Live Console." },
  ]}
/>

## What you can do now

<Recap
  items={[
    "Make the qu_dev_loop folder and every file by hand.",
    "Use fxmanifest.lua to load the exact files for edit, save, restart one resource, and verify one proof line.",
    "Add ensure qu_dev_loop to server.cfg and restart only that resource.",
    "Read the expected console output before changing the code."
  ]}
/>

## Try it yourself

<Exercise>
  Change one printed value, restart `qu_dev_loop`, rerun the test, and confirm the output changed.
</Exercise>

<Refs
  items={[
    { label: "FiveM resource manifest reference", href: "https://docs.fivem.net/docs/scripting-reference/resource-manifest/resource-manifest/" },
    { label: "FiveM native reference", href: "https://docs.fivem.net/natives/" },
    { label: "Lua 5.4 reference manual", href: "https://www.lua.org/manual/5.4/" }
  ]}
/>
