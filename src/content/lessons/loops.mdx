<LessonHero
  crumbs="Module 01 · Lesson 04"
  title="Loops: for, while, and when to stop"
  meta={[
    { label: "You'll build", value: "A qu_countdown resource that counts down from 5 to 1, then prints \"Launch!\"." },
    { label: "Time", value: "~18 minutes" },
    { label: "You need", value: "Lessons 01–03 done. A working FiveM server you can restart." },
    { label: "You'll learn", value: "Numeric for -> step values -> while loops -> break -> off-by-one errors -> the FiveM Wait rule" },
  ]}
>
  If you copy-paste the same line over and over, you probably need a loop. A loop runs a block of code again and again until you stop it. In FiveM, *knowing when to stop* matters a lot, because a bad loop can hang a server. By the end you will know `for` loops, `while` loops, and the safety habit that prevents your first crash.
</LessonHero>

<Prereq
  items={[
    "I can find my server resources folder.",
    "I can edit server.cfg and restart one resource.",
    "I can read txAdmin Live Console and F8 output."
  ]}
/>

<Callout variant="fivem" title="Build the proof, then change it">
  This lesson builds for loop and while loop countdowns as a working FiveM resource named `qu_loops`. Follow the folder names, file names, code, restart command, and expected output exactly.
</Callout>

## Build it

<StepGroup>
  <Step num={1} title="Make the resource folder" outcome="The server has one folder for this lesson.">
    Inside your server's `resources` folder, create this folder:

    ```text
    resources/qu_loops
    ```
  </Step>

  <Step num={2} title="Create the files" outcome="Every file named in the manifest exists.">
    Create this exact file layout:

    ```text
resources/qu_loops/
  fxmanifest.lua
  server.lua
    ```
  </Step>

  <Step num={3} title="Write fxmanifest.lua" outcome="FiveM knows which files to load.">
    Open `fxmanifest.lua` and paste this:

    ```lua
fx_version 'cerulean'
game 'gta5'

server_script 'server.lua'
    ```
  </Step>

  <Step num={4} title="Write the lesson code" outcome="The topic is now represented by runnable code.">
    Open `server.lua` and paste this:

    ```lua
RegisterCommand('countdown', function()
    for i = 5, 1, -1 do print('[qu_loops] ' .. i) end
    print('[qu_loops] Launch!')
end, true)

RegisterCommand('whilecount', function()
    local i = 3
    while i > 0 do
        print('[qu_loops] while ' .. i)
        i = i - 1
    end
end, true)
    ```
  </Step>

  <Step num={5} title="Start and test it" outcome="The expected proof appears in the correct console.">
    Open `server.cfg` and add this line:

    ```text
    ensure qu_loops
    ```

    Save, then run:

    ```text
    restart qu_loops
    ```

    Run this test:

    ```text
    countdown
    ```

    <ExpectedOutput>
      [qu_loops] 5
[qu_loops] 4
[qu_loops] 3
[qu_loops] 2
[qu_loops] 1
[qu_loops] Launch!
    </ExpectedOutput>
  </Step>
</StepGroup>

## If something went wrong

<FailureList
  items={[
    { symptom: "Resource qu_loops does not exist", fix: "Check the folder is inside resources and the ensure line uses the exact same name." },
    { symptom: "Could not load resource qu_loops", fix: "Open fxmanifest.lua and fix the first quote, brace, or file name error the console reports." },
    { symptom: "The command does not print", fix: "Run restart qu_loops, then run the test command in the console named in the step." },
    { symptom: "Client output is missing", fix: "Join the server first and read F8. Server output stays in txAdmin Live Console." },
  ]}
/>

## What you can do now

<Recap
  items={[
    "Make the qu_loops folder and every file by hand.",
    "Use fxmanifest.lua to load the exact files for for loop and while loop countdowns.",
    "Add ensure qu_loops to server.cfg and restart only that resource.",
    "Read the expected console output before changing the code."
  ]}
/>

## Run it live

No server needed for this part. Edit the Lua below and press RUN to execute it in your browser on real Lua 5.4.

<Sandbox initial={`for i = 1, 3 do
  print("tick " .. i)
end`} />

## Try it yourself

<Exercise>
  Change one printed value, restart `qu_loops`, rerun the test, and confirm the output changed.
</Exercise>

<Refs
  items={[
    { label: "FiveM resource manifest reference", href: "https://docs.fivem.net/docs/scripting-reference/resource-manifest/resource-manifest/" },
    { label: "FiveM native reference", href: "https://docs.fivem.net/natives/" },
    { label: "Lua 5.4 reference manual", href: "https://www.lua.org/manual/5.4/" }
  ]}
/>
