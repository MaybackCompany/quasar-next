<LessonHero
  crumbs="Module A3 · Operate & secure"
  title="Incident log runbook"
  meta={[
    { label: "You'll build", value: "A repeatable routine for turning a vague 'server broke' report into a logged, reproducible, fixable incident." },
    { label: "Time", value: "~20 minutes." },
    { label: "You need", value: "txAdmin access and a Discord channel for reports." },
    { label: "You'll learn", value: "Capture -> reproduce -> classify -> fix or escalate -> record, so problems stop recurring" },
  ]}
>
  When something breaks, the worst response is panic edits on the live server. This runbook turns chaos into a routine: capture the evidence, reproduce it, classify it, fix or escalate, and write it down so the same fire does not start twice. It pairs txAdmin logs with your Discord reports into one workflow.
</LessonHero>

<Prereq
  items={[
    "I can read txAdmin Live Console and the txData logs.",
    "I have a channel where players and staff report issues.",
    "I deploy changes through version control, not live edits (the Git lesson)."
  ]}
/>

<Callout variant="fivem" title="Where the evidence lives">
  txAdmin writes per-server logs under `txData/<your-server>/logs` (admin, fxserver, server). The Live Console shows what is happening now; the log files show what happened earlier. Both are your evidence.
</Callout>

## The runbook

<StepGroup>
  <Step num={1} title="Capture before you change anything" outcome="You have the error text, time, and who/what triggered it.">
    Grab the exact console error, the timestamp, the player or resource involved, and what they were doing. A screenshot of the report plus the matching console line is enough. Do not start editing yet.
  </Step>

  <Step num={2} title="Reproduce it" outcome="You can make the problem happen on demand.">
    Try to trigger it yourself on a test server or quiet time. A bug you can reproduce is a bug you can fix; a bug you cannot reproduce gets a watch entry, not a guess-fix.
  </Step>

  <Step num={3} title="Classify the severity" outcome="You know whether to fix now or schedule it.">
    Sort it: **critical** (server down, money/items exploitable), **major** (a core feature broken), or **minor** (cosmetic, rare). Critical jumps the queue; minor goes on the list.
  </Step>

  <Step num={4} title="Fix or escalate, then deploy safely" outcome="The fix reaches the server through your normal deploy loop.">
    If it is yours, fix it on your PC, test, and deploy with the Git loop. If it is a paid resource, open a support ticket with your captured evidence. Never patch directly on the live server.
  </Step>

  <Step num={5} title="Record the incident" outcome="A future you can find what happened and what fixed it.">
    Log one line per incident: date, symptom, cause, fix, and the resource/commit involved. Over time this becomes the fastest debugging tool you own.

    ```text
    2026-06-12 | shop dupe on rapid-click | client trusted for grant | added server cooldown + qty check | commit a1b2c3d
    ```
  </Step>
</StepGroup>

## Common mistakes

<FailureList
  items={[
    { symptom: "Edited the live server and made it worse", fix: "Stop. Roll back via Git, reproduce on a test setup, then deploy the fix through the normal loop." },
    { symptom: "Report says 'it broke', nothing else", fix: "Ask for time, what they did, and any error text. No reproduction means a watch entry, not a blind change." },
    { symptom: "Same bug keeps coming back", fix: "You fixed the symptom, not the cause. Use the incident log to find the real trigger." },
    { symptom: "Lost the error after a restart", fix: "Pull it from txData logs by timestamp. The Live Console clears; the log files do not." },
  ]}
/>

## What you can do now

<Recap
  items={[
    "Capture the error, time, and trigger before changing anything.",
    "Reproduce before you fix; watch what you cannot reproduce.",
    "Classify severity and fix or escalate accordingly.",
    "Deploy fixes through Git, never on the live server, and log every incident."
  ]}
/>

<Refs
  items={[
    { label: "Cfx.re docs · txAdmin", href: "https://docs.fivem.net/docs/resources/txAdmin/" },
    { label: "Cfx.re docs · Console and logging", href: "https://docs.fivem.net/docs/server-manual/server-commands/" }
  ]}
/>
