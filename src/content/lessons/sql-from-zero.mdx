<LessonHero
  crumbs="Module 07 · Lesson 21"
  title="SQL from zero: oxmysql"
  meta={[
    { label: "You'll ship", value: "A working resource that creates a table, inserts rows with safe parameterized queries, and reads them back." },
    { label: "Time", value: "~35 minutes" },
    { label: "You'll learn", value: "What a database is -> install oxmysql -> connection string -> CREATE TABLE -> INSERT with ? placeholders -> SELECT rows" },
    { label: "Prereqs", value: "Modules 01-06 complete. A running FiveM server with MariaDB or MySQL installed." },
  ]}
>
  Every script you have built so far forgets everything on restart. Cooldowns, bank balances, notes -- all gone. A database is the fix. It is a program that stores rows of data on disk and gives them back when you ask. This lesson teaches you just enough SQL to make your FiveM scripts remember things forever, using oxmysql as the bridge between Lua and the database.
</LessonHero>

<YouTube id="wFlXFoQV50U" title="SQL, JS, Lua, and Git masterclass" caption="Alphi: SQL, JS, Lua, and Git masterclass." />

<Prereq
  items={[
    "I can find my server resources folder.",
    "I can edit server.cfg and restart one resource.",
    "I can read txAdmin Live Console and F8 output."
  ]}
/>

<Callout variant="fivem" title="Build the proof, then change it">
  This lesson builds first oxmysql table, INSERT, and SELECT as a working FiveM resource named `qu_sql_from_zero`. Follow the folder names, file names, code, restart command, and expected output exactly.
</Callout>

## Build it

<StepGroup>
  <Step num={1} title="Make the resource folder" outcome="The server has one folder for this lesson.">
    Inside your server's `resources` folder, create this folder:

    ```text
    resources/qu_sql_from_zero
    ```
  </Step>

  <Step num={2} title="Create the files" outcome="Every file named in the manifest exists.">
    Create this exact file layout:

    ```text
resources/qu_sql_from_zero/
  fxmanifest.lua
  server.lua
    ```
  </Step>

  <Step num={3} title="Write fxmanifest.lua" outcome="FiveM knows which files to load.">
    Open `fxmanifest.lua` and paste this:

    ```lua
fx_version 'cerulean'
game 'gta5'

server_script '@oxmysql/lib/MySQL.lua'
server_script 'server.lua'

dependency 'oxmysql'
    ```

    Run this SQL before starting the resource:

    ```sql
CREATE TABLE IF NOT EXISTS qu_sql_from_zero_rows (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(80) NOT NULL,
  amount INT NOT NULL DEFAULT 0
);
    ```
  </Step>

  <Step num={4} title="Write the lesson code" outcome="The topic is now represented by runnable code.">
    Open `server.lua` and paste this:

    ```lua
RegisterCommand('dbtest', function(src)
    local id = MySQL.insert.await('INSERT INTO qu_sql_from_zero_rows (name, amount) VALUES (?, ?)', { 'lesson', 100 })
    local row = MySQL.single.await('SELECT * FROM qu_sql_from_zero_rows WHERE id = ?', { id })
    print('[qu_sql_from_zero] selected ' .. row.name .. ' ' .. row.amount)
    MySQL.update.await('UPDATE qu_sql_from_zero_rows SET amount = ? WHERE id = ?', { 150, id })
    print('[qu_sql_from_zero] updated ' .. id)
end, true)
    ```
  </Step>

  <Step num={5} title="Start and test it" outcome="The expected proof appears in the correct console.">
    Open `server.cfg` and add this line:

    ```text
    ensure qu_sql_from_zero
    ```

    Save, then run:

    ```text
    restart qu_sql_from_zero
    ```

    Run this test:

    ```text
    dbtest
    ```

    <ExpectedOutput>
      [qu_sql_from_zero] selected lesson 100
[qu_sql_from_zero] updated rowId
    </ExpectedOutput>
  </Step>
</StepGroup>

## If something went wrong

<FailureList
  items={[
    { symptom: "Resource qu_sql_from_zero does not exist", fix: "Check the folder is inside resources and the ensure line uses the exact same name." },
    { symptom: "Could not load resource qu_sql_from_zero", fix: "Open fxmanifest.lua and fix the first quote, brace, or file name error the console reports." },
    { symptom: "The command does not print", fix: "Run restart qu_sql_from_zero, then run the test command in the console named in the step." },
    { symptom: "Client output is missing", fix: "Join the server first and read F8. Server output stays in txAdmin Live Console." },
  ]}
/>

## What you can do now

<Recap
  items={[
    "Make the qu_sql_from_zero folder and every file by hand.",
    "Use fxmanifest.lua to load the exact files for first oxmysql table, INSERT, and SELECT.",
    "Add ensure qu_sql_from_zero to server.cfg and restart only that resource.",
    "Read the expected console output before changing the code."
  ]}
/>

## Try it yourself

<Exercise>
  Change one printed value, restart `qu_sql_from_zero`, rerun the test, and confirm the output changed.
</Exercise>

<Refs
  items={[
    { label: "FiveM resource manifest reference", href: "https://docs.fivem.net/docs/scripting-reference/resource-manifest/resource-manifest/" },
    { label: "FiveM native reference", href: "https://docs.fivem.net/natives/" },
    { label: "Lua 5.4 reference manual", href: "https://www.lua.org/manual/5.4/" }
  ]}
/>
