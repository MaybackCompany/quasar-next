<LessonHero
  title="QBCore and txAdmin — your first server"
  meta={[
    { label: "You'll build", value: "A working FiveM server with QBCore framework, txAdmin panel, MariaDB, and HeidiSQL — everything a beginner needs to host." },
    { label: "Time", value: "~45 minutes" },
    { label: "You need", value: "A Windows PC, internet connection, and a Cfx.re account for the license key." },
    { label: "You'll learn", value: "Installing MariaDB as a Windows service, downloading FiveM artifacts, setting up txAdmin, deploying QBCore via recipe, connecting HeidiSQL, and verifying the server is joinable." },
  ]}
>
  This is the complete beginner path to a running FiveM server — no skipped steps, no assumptions. By the end you have MariaDB running as a Windows service, txAdmin deployed with a QBCore recipe, HeidiSQL connected to your database, and players able to join. This is the lesson most beginners wish they had on day one.
</LessonHero>

<Prereq
  items={[
    "I am on Windows 10 or 11.",
    "I have a Cfx.re account at portal.cfx.re.",
    "I have at least 20GB free disk space.",
    "I have read the FiveM orientation lesson (fivem-2026-orientation).",
  ]}
/>

## Build it

<StepGroup>
  <Step num={1} title="Install MariaDB" outcome="MariaDB runs as a Windows service on 127.0.0.1:3306.">
    Download the latest Windows x86_64 MSI from https://mariadb.org/download/ and run the installer. Accept the defaults, but set a strong **root password** during install and store it in a password manager. Keep port **3306** and install it as a **Windows service**.

    After it finishes, confirm the service is running. Open PowerShell and run:

    ```powershell
Get-Service MariaDB*
    ```

    You want a row whose Status reads **Running**.

    <ExpectedOutput>
      Get-Service MariaDB* shows a MariaDB row with Status Running. MariaDB is listening on port 3306.
    </ExpectedOutput>
  </Step>

  <Step num={2} title="Download FiveM server artifacts" outcome="The FXServer files are in your server folder.">
    Go to https://runtime.fivem.net/artifacts/fivem/build_server_windows/master/

    Download the **latest recommended** build (green label, not the top "Latest" which may be unstable). Extract the zip to:

    ```text
C:\FXServer\
    ```

    Inside you should see `FXServer.exe`, `citizen/` folder, and other files.

    <ExpectedOutput>
      C:\FXServer\FXServer.exe exists. Running it starts a bare server.
    </ExpectedOutput>
  </Step>

  <Step num={3} title="Get your license key" outcome="You have a Cfx.re license key.">
    Go to https://portal.cfx.re/ → Servers → Create Server. Give it a name. Copy the license key — it starts with `cfxk_`. Save it somewhere you can paste from. You'll need it when txAdmin asks for it.

    Don't create a server.cfg yet — the txAdmin recipe will write one for you with all the right settings for QBCore.
  </Step>

  <Step num={4} title="Start the server and pick the QBCore recipe" outcome="txAdmin loads, QBCore is deployed, and the server is online.">
    Run `FXServer.exe`. On first boot, txAdmin opens at `http://localhost:40120`. Set a username and password. When it asks for a recipe, search for **QBCore** and select the official QBCore Framework recipe.

    txAdmin downloads QBCore, creates the `server.cfg`, sets up the database, and starts all resources. Paste your license key when prompted.

    <ExpectedOutput>
      txAdmin dashboard shows green status. The server is joinable at `127.0.0.1:30120`.
    </ExpectedOutput>
  </Step>

  <Step num={5} title="Connect HeidiSQL to your database" outcome="You can browse the QBCore database.">
    Download HeidiSQL from https://www.heidisql.com. Open it, click New Session:

    - Host: `127.0.0.1`
    - User: `root`
    - Password: the root password you set when installing MariaDB
    - Port: `3306`

    Click Open. You should see the QBCore database with tables like `players`, `vehicles`, `items`.

    <ExpectedOutput>
      HeidiSQL shows the QBCore database. You can browse player data, items, and server configuration tables.
    </ExpectedOutput>
  </Step>
</StepGroup>

## Common failures

| Exact error | Cause | Fix |
|---|---|---|
| MariaDB service won't start | Port 3306 already in use | Stop the other MySQL instance, or change MariaDB's port in `my.ini` and update HeidiSQL to match |
| `FXServer.exe` crashes immediately | Missing Visual C++ Redistributable | Install VC_redist.x64.exe from Microsoft |
| txAdmin recipe fails to download | Firewall blocking or GitHub rate limit | Wait a few minutes and retry. If persistent, download QBCore manually |
| `ensure` command not found in HeidiSQL console | HeidiSQL doesn't run FiveM commands | Use txAdmin Live Console for FiveM commands, HeidiSQL for SQL only |
