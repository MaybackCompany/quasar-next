<LessonHero
  crumbs="Module 01 · Lesson 03"
  title="If / else / elseif: how code makes decisions"
  meta={[
    { label: "You'll build", value: "A qu_gate resource that greets players differently based on their level." },
    { label: "Time", value: "~15 minutes" },
    { label: "You need", value: "Lessons 01 and 02 complete. A running FiveM server. A text editor." },
    { label: "You'll learn", value: "if -> elseif -> else -> comparison operators -> and / or / not -> truthy vs falsy" },
  ]}
>
  So far your scripts have run from top to bottom and always done the same thing. This lesson changes that. You will teach Lua to *choose*: look at a value, ask a question about it, and take a different path based on the answer. This is where a simple script starts becoming a real program.
</LessonHero>

<Prereq
  items={[
    "I can find my server resources folder.",
    "I can edit server.cfg and restart one resource.",
    "I can read txAdmin Live Console and F8 output."
  ]}
/>

<Callout variant="fivem" title="Build the proof, then change it">
  This lesson builds if, elseif, else rank gate as a working FiveM resource named `qu_if_else_elseif`. Follow the folder names, file names, code, restart command, and expected output exactly.
</Callout>

## Build it

<StepGroup>
  <Step num={1} title="Make the resource folder" outcome="The server has one folder for this lesson.">
    Inside your server's `resources` folder, create this folder:

    ```text
    resources/qu_if_else_elseif
    ```
  </Step>

  <Step num={2} title="Create the files" outcome="Every file named in the manifest exists.">
    Create this exact file layout:

    ```text
resources/qu_if_else_elseif/
  fxmanifest.lua
  server.lua
    ```
  </Step>

  <Step num={3} title="Write fxmanifest.lua" outcome="FiveM knows which files to load.">
    Open `fxmanifest.lua` and paste this:

    ```lua
fx_version 'cerulean'
game 'gta5'

server_script 'server.lua'
    ```
  </Step>

  <Step num={4} title="Write the lesson code" outcome="The topic is now represented by runnable code.">
    Open `server.lua` and paste this:

    ```lua
RegisterCommand('gate', function(_, args)
    local level = tonumber(args[1]) or 0
    local rank
    if level >= 20 then rank = 'owner'
    elseif level >= 10 then rank = 'admin'
    elseif level >= 1 then rank = 'member'
    else rank = 'guest' end
    print(('[qu_if_else_elseif] level %d rank %s'):format(level, rank))
end, true)
    ```
  </Step>

  <Step num={5} title="Start and test it" outcome="The expected proof appears in the correct console.">
    Open `server.cfg` and add this line:

    ```text
    ensure qu_if_else_elseif
    ```

    Save, then run:

    ```text
    restart qu_if_else_elseif
    ```

    Run this test:

    ```text
    gate 12
    ```

    <ExpectedOutput>
      [qu_if_else_elseif] level 12 rank admin
    </ExpectedOutput>
  </Step>
</StepGroup>

## If something went wrong

<FailureList
  items={[
    { symptom: "Resource qu_if_else_elseif does not exist", fix: "Check the folder is inside resources and the ensure line uses the exact same name." },
    { symptom: "Could not load resource qu_if_else_elseif", fix: "Open fxmanifest.lua and fix the first quote, brace, or file name error the console reports." },
    { symptom: "The command does not print", fix: "Run restart qu_if_else_elseif, then run the test command in the console named in the step." },
    { symptom: "Client output is missing", fix: "Join the server first and read F8. Server output stays in txAdmin Live Console." },
  ]}
/>

## What you can do now

<Recap
  items={[
    "Make the qu_if_else_elseif folder and every file by hand.",
    "Use fxmanifest.lua to load the exact files for if, elseif, else rank gate.",
    "Add ensure qu_if_else_elseif to server.cfg and restart only that resource.",
    "Read the expected console output before changing the code."
  ]}
/>

## Run it live

No server needed for this part. Edit the Lua below and press RUN to execute it in your browser on real Lua 5.4.

<Sandbox initial={`local cash = 250
if cash >= 200 then
  print("you can afford it")
else
  print("not enough cash")
end`} />

## Try it yourself

<Exercise>
  Change one printed value, restart `qu_if_else_elseif`, rerun the test, and confirm the output changed.
</Exercise>

<Refs
  items={[
    { label: "FiveM resource manifest reference", href: "https://docs.fivem.net/docs/scripting-reference/resource-manifest/resource-manifest/" },
    { label: "FiveM native reference", href: "https://docs.fivem.net/natives/" },
    { label: "Lua 5.4 reference manual", href: "https://www.lua.org/manual/5.4/" }
  ]}
/>
