<LessonHero
 crumbs="Module A3 · Operate & secure"
 title="Artifact update SOP"
 meta={[
 { label: "You'll build", value: "A safe, repeatable routine for updating your FXServer artifact: record the build you run, back it up, update to the latest, test, and roll back if it breaks." },
 { label: "Time", value: "~15 minutes to set up; minutes per update after." },
 { label: "You need", value: "A running server and a note of the build number it runs today." },
 { label: "You'll learn", value: "Why artifacts expire -> the support window -> a backup-first update routine -> how to roll back fast" },
 ]}
>
 An **artifact** is the FXServer build you downloaded. FXServer is the server program itself. Artifacts are not forever. Cfx.re supports them on a clock, and an old artifact eventually makes your server unjoinable from the in-game browser. So you do need to update. But you do not want to grab a new build the night before a launch and find out live that it broke something. This SOP makes updating boring. Record the build you run, back it up, install the latest, test it, and keep a one-step rollback ready.
</LessonHero>

<Prereq
 items={[
 "I have a running server and I know its current artifact build number.",
 "I recorded that build number in a small note next to the server.",
 "I can stop and start the server safely."
 ]}
/>

<Callout variant="fivem" title="The support clock">
 Cfx.re supports each artifact for a limited time. The exact window changes over time, so do not memorize a number. The rough model: a build stays supported for a while, then ages out, and once it is too old the server browser refuses to let players join it. Before you plan an update, read the current window in the support article linked at the bottom of this lesson. The takeaway is the same either way. Update on your own schedule, not in a panic, and do not let an artifact rot until the browser kicks your players.
</Callout>

## The update routine

<StepGroup>
 <Step num={1} title="Record the build you run today" outcome="You know exactly what to roll back to.">
 Keep a tiny note next to the server. **This note is your rollback plan.** Without it you cannot get back to a known-good build cleanly.

 ```text
 Current build: <number>
 Known-good: yes
 Updated: <date>
 Rollback build: <previous number>
 ```

 "Known-good" means you have actually joined this build and it worked. The "rollback build" is the one you ran before this one, in case the current one ever turns out to be bad.
 </Step>

 <Step num={2} title="Back up the artifact binaries before you touch anything" outcome="The exact build you run today is saved off to one side.">
 Your server folder has two halves. The **artifact binaries** are the FXServer files you downloaded and extracted: `FXServer.exe`, `run.cmd` (or `run.sh` on Linux), the `citizen` folder, and the rest of the runtime. Your **server-data** is everything you own: the `resources` folder, `server.cfg`, your database, your `.env`. An update only swaps the binaries. It must never touch server-data.

 Copy the current binaries to a dated backup, for example a folder named `fxserver-2026-06-12`. **That copy is your undo button** if the new build misbehaves. Leave `resources`, `server.cfg`, and your database exactly where they are.
 </Step>

 <Step num={3} title="Install the latest build" outcome="The server runs the current build with your config untouched.">
 Open the Cfx.re Windows artifacts page and download the **newest build**. It sits at the very **top** of the list.

 ```text
 https://runtime.fivem.net/artifacts/fivem/build_server_windows/master/
 ```

 Ignore the green "recommended" row further down the page. It is older and lags behind, and you want the current build. On Linux, use the same top-of-list build from `https://runtime.fivem.net/artifacts/fivem/build_proot_linux/master/`.

 Stop FXServer. Extract the new archive into a fresh artifact directory, then point your existing txAdmin and server-data setup at that new directory. Replacing the whole directory avoids a mixed build with stale files left over. **Never extract over `resources`, `server.cfg`, `txData`, or the database.** Then start the server.

 If a brand-new build ever misbehaves after you update, that is the only time you drop to the build directly below it. Run the latest by default. Fall back only if it breaks.
 </Step>

 <Step num={4} title="Smoke-test, then mark known-good or roll back" outcome="You either confirm the build or revert in one step.">
 Run the same checklist every time so a regression cannot hide. **F8** is the in-game client console you open by pressing F8 while the FiveM game window is in focus.

 - **Console clean.** Watch the FXServer console as resources load. No new red errors, and no resource that failed to start.
 - **Connect.** Join your own server from the FiveM client. If it is running on the same PC, press F8 in the game and type the command below (30120 is the default FXServer game port). You should reach character select or spawn, not a load loop.

 ```text
 connect 127.0.0.1:30120
 ```

 - **Spawn.** Confirm your ped (your player character) actually spawns into the world.
 - **Framework health check.** Use one documented, non-destructive action you already verified on the previous build, such as opening character selection or reading player and job data. Do not assume `/car`, `/setjob`, or `/giveitem` exists or is safe on every framework.
 - **resmon.** `resmon` is a developer-only client command that shows each resource's CPU cost. It only works if you launched the FiveM client in developer mode (add the launch argument `+set moo 31337`). On a normal client, `resmon 1` returns `Access denied for command resmon`. With developer mode on, open F8, type `resmon 1`, and confirm no resource is suddenly burning CPU it did not burn before. `resmon 0` closes it. A build change can shift timing. If you cannot run developer mode, watch txAdmin's performance panel instead.

 ```text
 resmon 1
 ```

 If every line passes, update the note to the new build and mark it known-good. If any line fails, **do not debug live.** Stop the server, restore the dated artifact backup from Step 2, start it again, and you are back on the build you trusted this morning.

 <ExpectedOutput>
 The startup banner reads `FXServer-master v1.0.0.XXXX windows` (or `linux`), where `XXXX` is your artifact build number; there are no new resource load errors, your join succeeds, and (if you have developer mode) resmon shows normal timings.
 </ExpectedOutput>
 </Step>
</StepGroup>

## Common mistakes

<FailureList
 items={[
 { symptom: "Players kicked: artifact outdated / End of Support", fix: "Your build is past the support window. Update to the latest build from the top of the artifacts list, following this SOP." },
 { symptom: "New build breaks a resource", fix: "Restore the dated artifact binaries you saved in Step 2 and start the server. Your resources and config were never touched, so you are back on the known-good build. Then trial the new build on a separate test server before retrying." },
 { symptom: "No idea which build was working", fix: "Keep the build note from Step 1 updated every time. Without it you cannot roll back cleanly." },
 { symptom: "Server won't start after updating", fix: "The new artifact may need a newer Visual C++ Redistributable, or it broke an old resource. Restore from your dated backup, then read the release notes at runtime.fivem.net for breaking changes before retrying." },
 { symptom: "Resources stopped working after the update", fix: "A new build can change a default or deprecate a feature. Check the release notes. Common causes: new default convars (config switches the server reads at startup), removed natives, or stricter OneSync scoping (OneSync is the modern server-side state and entity system; a new build can tighten how it scopes entities to players). Restore the backup if you cannot fix it quickly." },
 { symptom: "Game build mismatch after update", fix: "Artifact builds and GTA content builds are separate numbers. Check sv_enforceGameBuild against Cfx.re's supported game-build list and your asset requirements, then perform a full restart. Do not use the FXServer artifact number here." },
 ]}
/>

## What you can do now

<Recap
 items={[
 "Explain why artifacts expire, and check the current support window in the Cfx.re support article instead of trusting a memorized number.",
 "Record the current build in a note before every update, with the previous build as your rollback.",
 "Tell the artifact binaries apart from your server-data, and back up only the binaries before swapping.",
 "Install the latest build from the top of the artifacts list, run the connect / spawn / framework / resmon / clean-console smoke test, and roll back to the dated backup if any line fails."
 ]}
/>

<Refs
 items={[
 { label: "Cfx.re support · Outdated / End of Support artifacts", href: "https://support.cfx.re/hc/en-us/articles/12242287993500-Server-artifact-version-is-outdated-or-End-of-Support-End-of-Life-error-messages-when-trying-to-join-a-server" },
 { label: "FiveM Windows server artifacts (download the latest at the top)", href: "https://runtime.fivem.net/artifacts/fivem/build_server_windows/master/" },
 { label: "Cfx.re supported GTA game builds", href: "https://docs.fivem.net/docs/server-manual/server-commands/#sv_enforcegamebuild-build" }
 ]}
/>
