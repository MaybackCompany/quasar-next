<LessonHero
  crumbs="Cheatsheets"
  title="One-page references, pinned beside the lesson."
  meta={[

  ]}
>
  Each card is a starter reference: the smallest piece of the topic, with a link straight to the lesson where it is taught. Open the card while you read. Pin the lesson when the topic clicks.
</LessonHero>

## Pick the topic, *open the lesson.*

These are starter references, not the full manual. The card gives you the shape of the topic. The lesson teaches you why.

<LinkCardGrid>
  <LinkCard href="/cheatsheets/lua-syntax" title="Variables, numbers, strings" desc="The shape of local, types, concatenation with .., and where Lua differs from JavaScript or Python." eyebrow="Lua syntax" />
  <LinkCard href="/lessons/if-else-elseif" title="If, else, elseif" desc="Boolean truth, comparison operators, and the four conditional shapes you will reuse forever." eyebrow="Control flow" />
  <LinkCard href="/cheatsheets/tables" title="Lists, maps, and pairs" desc="Numeric vs string keys, ipairs vs pairs, and the one mistake that creates silent bugs." eyebrow="Tables" />
  <LinkCard href="/cheatsheets/resource-skeleton" title="fxmanifest, client, server" desc="The minimum folder layout. What goes in fx_version, where shared lives, and which scripts must never be in client_scripts." eyebrow="Resource skeleton" />
  <LinkCard href="/lessons/client-server-shared" title="The wall, in one card" desc="Which side runs which file, what shared can see, and the rule for &quot;do not trust the client&quot; you will repeat in every lesson after." eyebrow="Client · server · shared" />
  <LinkCard href="/cheatsheets/events" title="TriggerEvent, server, client" desc="The four event verbs in one place: TriggerEvent, TriggerServerEvent, TriggerClientEvent, and the secured net wrappers." eyebrow="Events" />
  <LinkCard href="/lessons/callbacks" title="Ask, wait, answer" desc="The two callback patterns: ox_lib server callbacks and the older async tableback. When to pick which." eyebrow="Callbacks" />
  <LinkCard href="/cheatsheets/ox-lib-ui" title="Notify, dialog, context, input" desc="The five UI primitives every modern script reaches for: notify, alertDialog, inputDialog, context menu, progressBar." eyebrow="ox_lib" />
  <LinkCard href="/lessons/frameworks" title="QBCore, ESX, Qbox map" desc="Equivalent calls side by side: get player, get money, get job, give item. Which one your server actually has." eyebrow="Frameworks" />
  <LinkCard href="/cheatsheets/ox-target" title="Zone, entity, model, bone" desc="Four ways to attach an interaction. The shape of an option, icon names, canInteract guards, and the cleanup path." eyebrow="ox_target" />
  <LinkCard href="/cheatsheets/oxmysql" title="oxmysql without injection" desc="Five queries: insert, update, select one, select many, delete. Always with ? placeholders and a params array. Never with string concatenation." eyebrow="Parameterized SQL" />
  <LinkCard href="/cheatsheets/entity-cleanup" title="Spawn, register, delete" desc="The registry pattern: every spawned entity gets an id, a cleanup path, and a stop handler. Zero leaks at restart." eyebrow="Entity cleanup" />
  <LinkCard href="/lessons/vehicles-spawn-modify-store" title="Spawn, modify, store" desc="Request the model, network the vehicle, set props, store the plate. Server validates who may request." eyebrow="Vehicles" />
  <LinkCard href="/lessons/debug-class" title="What the error means" desc="The plain-English map of the four errors beginners actually hit: nil, missing exports, wrong-side calls, SQL failures. Pin this before lesson 03." eyebrow="Debug checklist" />
  <LinkCard href="/resources/script-planning" title="Plan before you code" desc="The fill-in-the-blanks spec from the build lessons: data, events, edges, errors, KPIs. Print or copy." eyebrow="Spec template" />
  <LinkCard href="/templates" title="Copy-safe starter files" desc="Seven copy-safe starters that pair with the cheatsheets above: resource skeleton, event gateway, SQL save and load, entity registry, vehicle spawn, ox_target, debug capture." eyebrow="Templates hub" />
</LinkCardGrid>
