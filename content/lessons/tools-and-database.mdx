<LessonHero
 crumbs="Step 02"
 title="Install tools, account, and database"
 meta={[
 { label: "You'll do", value: "Install VS Code, an archive tool, WinSCP, MariaDB, and HeidiSQL. Turn on file extensions. Create a Cfx.re account and a free license key." },
 { label: "Time", value: "~30 minutes. Most of it is installers and the account email." },
 { label: "You need", value: "Step 01 done, admin rights on Windows, and an email you can check now." },
 { label: "Result", value: "You are ready to install the latest FiveM artifacts and pick a txAdmin recipe in Step 03." },
 ]}
>
 This step installs the tools FiveM owners use every day. You also create your free **Cfx.re license key** and set up the **MariaDB** database before txAdmin asks for it. A license key is the free code that lets your server run. MariaDB is the database engine that stores your players, money, and inventory. Keep one picture in your head: MariaDB is the engine, **HeidiSQL** is the window you look through it with, and `oxmysql` is the community add-on (built by Overextended) that lets your scripts talk to the engine later.
</LessonHero>

<Prereq
 items={[
 "My FiveM client opens and signs in (Step 01 is done).",
 "I have admin rights to install Windows programs.",
 "I can open my email right now to verify an account.",
 "I have a Windows PC for the playable test (the FiveM client is Windows-only)."
 ]}
/>

<Callout variant="fivem" title="Owner mental model">
 MariaDB is the database engine. It stores players, money, vehicles, jobs, and inventory. HeidiSQL is the app you use to see and edit that data safely. `oxmysql` is a separate community resource from Overextended. A resource is a folder of code you drop into your server's `resources` folder. It does not ship with FiveM. You add it later and start it like any other script. Install each tool, read the proof line, then move on.
</Callout>

## The tools you'll install

Use the official downloads only. Do not use random mirrors. The editor edits your files. MariaDB stores your data. HeidiSQL is the window into that data. oxmysql is the add-on your scripts use to reach the data later.

<TechLinks ids={["vscode", "7zip", "winrar", "winscp", "mariadb", "heidisql", "oxmysql"]} />

## Build it

<StepGroup>
 <Step num={1} title="Install VS Code and turn on file extensions" outcome="Right-clicking a folder shows Open with Code, and File Explorer shows full file names.">
 Download VS Code from [code.visualstudio.com/download](https://code.visualstudio.com/download). Run the **Windows User Installer** and accept the defaults. On the **Additional Tasks** screen, tick both **Open with Code** context-menu options.

 Then open File Explorer. Go to the **View** menu and turn on **File name extensions** and **Hidden items**.

 **Turn on extensions or a broken file will hide from you.** A bad config file often looks like `server.cfg.txt`. FiveM needs `server.cfg`. With extensions hidden, you cannot see the extra `.txt`.

 To prove it, create a file on your Desktop:

 ```text
 Desktop\proof.cfg
 ```

 <ExpectedOutput>
 Right-clicking a folder shows "Open with Code", and File Explorer shows the full name proof.cfg (not proof.cfg.txt and not proof).
 </ExpectedOutput>
 </Step>

 <Step num={2} title="Install an archive tool" outcome="You can extract the .7z and .zip files FiveM ships artifacts in.">
 FiveM artifacts come as compressed archives. An artifact is the FiveM server program you download in Step 03. Install **7-Zip** from [7-zip.org](https://www.7-zip.org/) (free) or **WinRAR** from [win-rar.com](https://www.win-rar.com/). Use the 64-bit Windows version and accept the defaults.

 To prove it, right-click any `.7z` or `.zip` file. The menu now shows an **Extract** option from your archive tool.

 <ExpectedOutput>
 A .7z or .zip file shows an Extract option in the right-click menu.
 </ExpectedOutput>
 </Step>

 <Step num={3} title="Install WinSCP for later" outcome="WinSCP is installed and ready for VPS file transfers in a future lesson.">
 You do not need this for a local server. Install it now to save time later. WinSCP moves files to a VPS (a rented Windows or Linux machine you run your server on). Download WinSCP from [winscp.net/eng/download.php](https://winscp.net/eng/download.php), run the installer with default settings, and **do not connect to anything yet**.

 <ExpectedOutput>
 WinSCP opens to a login screen. Close it for now.
 </ExpectedOutput>
 </Step>

 <Step num={4} title="Create your Cfx.re account and free license key" outcome="The key is stored in a password manager and ready for txAdmin.">
 Every FiveM server needs a license key, **even a local test server**. The key lets your server connect to the Cfx.re platform and run. It also ties your server to your Cfx.re account. The key is **free**. You get it from the Cfx.re **Portal** at [portal.cfx.re](https://portal.cfx.re/).

 The old `keymaster.fivem.net` address now redirects to `portal.cfx.re`. They are the same Cfx.re system, so either link lands you in the right place.

 1. Create or verify your account at [forum.cfx.re](https://forum.cfx.re/) and confirm the email.
 2. Go to the portal at [portal.cfx.re](https://portal.cfx.re/).
 3. Open **Server Management** and create a new license.
 4. Copy the key into your password manager. If you must use a temporary text file during setup, keep it outside the repository, restrict it to your Windows account, and delete it after txAdmin is configured.

 **Treat the key like a password.** Never paste it in Discord, GitHub, screenshots, or videos. If it leaks, revoke it on the portal and generate a new one.

 <ExpectedOutput>
 Your password manager contains the new key, ready to paste into txAdmin without showing it in a screenshot or commit.
 </ExpectedOutput>
 </Step>

 <Step num={5} title="Install MariaDB" outcome="MariaDB runs as a Windows service on 127.0.0.1:3306.">
 Download the latest Windows x86_64 MSI from [mariadb.org/download](https://mariadb.org/download/) and run the installer.

 1. Set a strong **root password**. Store it in a password manager, not a Desktop text file.
 2. Enable **utf8mb4** as the default character set, not the legacy 3-byte `utf8`. utf8mb4 is the full 4-byte Unicode set. It stores any player name or emoji, and it matches the collation you set in Step 6. Modern FiveM schemas (ESX Legacy, Qbox) assume it.
 3. Keep port **3306** and install it as a **Windows service**. A Windows service is a program that runs in the background with no window.

 Confirm the service is running. Open PowerShell and run:

 ```powershell
 Get-Service MariaDB*
 ```

 <ExpectedOutput>
 Illustrative — your exact service name may differ by MariaDB version. What matters is a row whose Status reads Running:

 Status   Name      DisplayName
 ------   ----      -----------
 Running  MariaDB   MariaDB
 </ExpectedOutput>

 If the service has a slightly different name, open Windows **Services** (run `services.msc`) and look for **MariaDB**. The proof you want is the status **Running**.
 </Step>

 <Step num={6} title="Install HeidiSQL and create the database" outcome="HeidiSQL connects to MariaDB and shows a database named fivem.">
 Download HeidiSQL from [heidisql.com/download.php](https://www.heidisql.com/download.php) and install it. Then create a new session:

 1. Name the session `local FiveM`.
 2. Host `127.0.0.1`, user `root`, password from your password manager, port `3306`.
 3. Open the session, then create a database named `fivem`.
 4. Use collation `utf8mb4_unicode_ci`. This pairs with the utf8mb4 charset from Step 5. Player names with accents or emoji then store and sort correctly instead of breaking on insert.

 To prove the connection, do this inside HeidiSQL (the database app you installed, not PowerShell and not the FiveM console). Click the `fivem` database, open a query tab with the Query button or Ctrl+T, and run:

 ```text
 SELECT DATABASE();
 ```

 <ExpectedOutput>
 The result grid shows a single value: fivem
 </ExpectedOutput>

 Here is what your screen looks like once it works. The session is open on the left, the `fivem` database sits in the tree, and the query you ran returns its result in the grid below.

 <Mockup app="HeidiSQL · local FiveM">
 ```text
 TREE (left)         QUERY TAB (center, top)
 ─────────────────   ─────────────────────────────
 ▾ local FiveM       SELECT DATABASE();
   ▾ fivem
       players       [ F9 / blue play ▶ runs it ]
       characters
   ▸ information_schema
 ```

 <MockTable
 head={["DATABASE()"]}
 rows={[["fivem"]]}
 caption="Result grid (center, bottom). One row, one column: proof HeidiSQL is pointed at your fivem database."
 />
 </Mockup>
 </Step>
</StepGroup>

## How MariaDB and HeidiSQL relate (and why scripts care)

You installed two things that sound like one thing. They are not. Knowing which is which is the difference between fixing a database problem in ten seconds and uninstalling the wrong program at midnight.

### MariaDB is the engine, HeidiSQL is the window

**MariaDB** is the database engine. It is a background Windows service with no real screen of its own. It listens on `127.0.0.1:3306`. It holds your tables on disk. It answers the SQL it is handed. When you ran `Get-Service MariaDB*` in Step 5 and saw **Running**, that service is the engine doing its job whether or not any window is open.

**HeidiSQL** is a separate program. It connects to that engine and draws it for you: the session on the left, the database tree, the query editor, the result grid. Close HeidiSQL and your data is untouched, because the data never lived in HeidiSQL. It lives in MariaDB.

This is why the two install steps are separate. Step 5 installs the engine and proves it is running. Step 6 installs the window and proves it can see in. If HeidiSQL says "connection refused", the window is fine but the engine behind it is stopped. If HeidiSQL says "Access denied", the window reached the engine but handed it the wrong password.

### How a script reaches the same database

Your FiveM scripts never open HeidiSQL, and they are not MariaDB either. They reach the engine through a third piece you install later as a resource: **oxmysql**, the community connector built by Overextended. You drop it into `resources` and `ensure` it in `server.cfg`. `ensure` is the server.cfg command that starts a resource. Then you point oxmysql at the same engine HeidiSQL connects to, using one convar.

A convar is a config value you set in `server.cfg`. The convar here is `mysql_connection_string`:

```cfg
# server.cfg — oxmysql reads this convar, then your scripts query through it
set mysql_connection_string "mysql://root:YOUR_PASSWORD@localhost/fivem?charset=utf8mb4"
ensure oxmysql
```

Read that string left to right. It is the same four facts you typed into the HeidiSQL session. User `root`. Your password. Host `localhost` (the same machine as `127.0.0.1`). The `fivem` database.

One catch the oxmysql docs warn about. This is a URL. So if your root password contains any of the characters `@ # / : ? & = + $`, you must URL-encode them here, or oxmysql will misread the string and fail to connect. For example, `@` becomes `%40` and `#` becomes `%23`.

If that feels fragile, oxmysql also accepts a key=value form that avoids the problem:

```cfg
set mysql_connection_string "user=root;password=YOUR_PASSWORD;host=localhost;database=fivem;charset=utf8mb4"
```

**HeidiSQL connects so a human can look. oxmysql connects so scripts can read and write.** Both knock on the same door at `127.0.0.1:3306`. That shared door is the whole point of getting the database right now, before txAdmin or any framework asks for it.

So the chain a script follows is one hop longer than the one you do by hand:

```text
You          -> HeidiSQL -> MariaDB (127.0.0.1:3306) -> fivem database
A FiveM script -> oxmysql  -> MariaDB (127.0.0.1:3306) -> fivem database
```

When you reach the hands-on lesson [HeidiSQL hands-on](/lessons/heidisql-hands-on), you will use that same window to import a framework's tables and browse player rows. Everything you build there is the human path into the engine your scripts hit through oxmysql.

<Checkpoint question="HeidiSQL is closed, but your script still cannot read the database. Where is the problem most likely to be, and where is it definitely not?">
 Not in HeidiSQL. HeidiSQL is only a window for humans. Closing it changes nothing for scripts, because scripts go through oxmysql, not HeidiSQL. The problem is in the path your script actually uses. Either the MariaDB service is stopped (run `Get-Service MariaDB*` and confirm Running), or the `mysql_connection_string` in `server.cfg` has the wrong user, password, host, or database name. Both HeidiSQL and oxmysql hit the same engine at `127.0.0.1:3306`. So if HeidiSQL can connect with a given user and password, point `mysql_connection_string` at exactly those values.
</Checkpoint>

<Callout title="Go deeper">
 For a hands-on tour of HeidiSQL itself, navigating the tree, running a table, and importing a script's SQL, see [HeidiSQL hands-on](/lessons/heidisql-hands-on).
</Callout>

## If something went wrong

<FailureList
 items={[
 { symptom: "You saved server.cfg.txt instead of server.cfg", fix: "Turn on File name extensions in File Explorer, then rename the file so it ends in .cfg with no .txt." },
 { symptom: "MariaDB installer says port 3306 is already in use", fix: "Another database is already on 3306. Open services.msc, find that service (or run Get-Service for MySQL or MariaDB), stop the conflicting one, then reinstall on port 3306." },
 { symptom: "MariaDB install fails with an error", fix: "You are missing the Visual C++ Redistributable. Download VC_redist.x64.exe from Microsoft, install it, then run the MariaDB installer again." },
 { symptom: "HeidiSQL says Access denied for user 'root'", fix: "The password is wrong. Retrieve it from your password manager. If it is genuinely lost, follow MariaDB's official password-reset procedure rather than reinstalling over data." },
 { symptom: "HeidiSQL says connection refused", fix: "The MariaDB service is stopped. Run Get-Service MariaDB* and start it from Windows Services (services.msc) if it is not Running." },
 { symptom: "You created the fivem database but cannot see it in HeidiSQL", fix: "HeidiSQL has not refreshed. Right-click the session at the top of the tree and choose Refresh, and the database appears." },
 { symptom: "HeidiSQL connects fine but a script later cannot reach the database", fix: "Scripts go through oxmysql, not HeidiSQL. Make the mysql_connection_string convar in server.cfg use the exact user, password, host, and database (fivem) that HeidiSQL connects with. Closing HeidiSQL never affects scripts." },
 { symptom: "The Cfx.re portal will not create a key", fix: "Verify your account email and sign in to portal.cfx.re with the same account. Try a private window if old cookies are stuck." },
 ]}
/>

## What you can do now

<Recap
 items={[
 "Use VS Code to edit .cfg and .lua files, with real file extensions visible.",
 "Extract FiveM artifact archives with 7-Zip or WinRAR.",
 "Generate a free Cfx.re license key from the portal and store it safely.",
 "Run MariaDB as a service and confirm it with Get-Service MariaDB*.",
 "Connect HeidiSQL to MariaDB and create the fivem database for txAdmin to use in Step 03.",
 "Explain that MariaDB is the engine, HeidiSQL is the window humans look through, and oxmysql is the connector scripts use, all reaching the same database at 127.0.0.1:3306."
 ]}
/>

## Try it yourself

<Exercise>
 Create a file on your Desktop named `hello.cfg` using VS Code, then check File Explorer. Confirm it shows exactly `hello.cfg`, not `hello.cfg.txt` and not `hello`. This proves your file extensions are visible before you ever touch a real server.cfg.
</Exercise>

<Refs
 items={[
 { label: "Cfx.re docs · Setting up a server", href: "https://docs.fivem.net/docs/server-manual/setting-up-a-server/" },
 { label: "Cfx.re portal · license keys", href: "https://portal.cfx.re/" },
 { label: "MariaDB downloads", href: "https://mariadb.org/download/" },
 { label: "HeidiSQL downloads", href: "https://www.heidisql.com/download.php" }
 ]}
/>
